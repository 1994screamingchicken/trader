# Kraken Lua Trader Configuration
# ================================

[general]
# Directory containing Lua trading scripts
scripts_directory = "scripts"
# Main loop tick interval in milliseconds
tick_interval_ms = 200
# Paper trading mode (no real orders placed)
paper_trading = true

[kraken]
# Your Kraken API key (leave empty for paper trading)
api_key = ""
# Your Kraken API secret (leave empty for paper trading)
api_secret = ""
# REST API endpoint
rest_url = "https://api.kraken.com"
# Public WebSocket endpoint
ws_url = "wss://ws.kraken.com"
# Authenticated WebSocket endpoint
ws_auth_url = "wss://ws-auth.kraken.com"

[risk]
# Maximum position size per asset (in base currency units)
max_position_size = 1.0
# Maximum total loss across all scripts before emergency halt (USD)
max_total_loss = 1000.0
# Maximum loss per individual script before stopping it (USD)
max_script_loss = 200.0
# Maximum number of open orders across all scripts
max_open_orders = 20
# Emergency stop if equity drops below this value (0 = disabled)
equity_floor = 0.0
# Enable/disable position limit enforcement
enforce_position_limits = true
# Enable/disable loss limit enforcement
enforce_loss_limits = true

[rate_limit]
# Maximum API calls per second (global)
max_calls_per_second = 1
# Maximum API calls per minute (global)
max_calls_per_minute = 45
# Cooldown period in seconds after hitting rate limit
cooldown_seconds = 30
# Per-script rate limit (calls per minute)
per_script_calls_per_minute = 10

[logging]
# Log level: trace, debug, info, warn, error, critical
level = "info"
# Log file path
log_file = "trader.log"
# Output to console
console_output = true
# Output to file
file_output = true
# Maximum log file size in MB before rotation
max_file_size_mb = 50
# Maximum number of rotated log files to keep
max_files = 5

# Script definitions
# Each [[scripts]] entry defines a trading script to load
# Top 10 cryptocurrencies by market cap on Kraken

[[scripts]]
name = "momentum_btc"
path = "scripts/simple_momentum.lua"
auto_start = true
[scripts.parameters]
pair = "XBT/USD"
threshold = "0.5"
quantity = "0.001"

[[scripts]]
name = "mean_reversion_eth"
path = "scripts/mean_reversion.lua"
auto_start = true
[scripts.parameters]
pair = "ETH/USD"
lookback = "20"
deviation = "2.0"
quantity = "0.01"

[[scripts]]
name = "ema_crossover_sol"
path = "examples/ema_crossover.lua"
auto_start = true
[scripts.parameters]
pair = "SOL/USD"
fast_period = "9"
slow_period = "21"
quantity = "0.5"

[[scripts]]
name = "grid_trader_xrp"
path = "scripts/grid_trader.lua"
auto_start = true
[scripts.parameters]
pair = "XRP/USD"
grid_levels = "10"
grid_spacing = "0.02"
quantity = "50"

[[scripts]]
name = "momentum_ada"
path = "scripts/simple_momentum.lua"
auto_start = true
[scripts.parameters]
pair = "ADA/USD"
threshold = "0.8"
quantity = "100"

[[scripts]]
name = "mean_reversion_dot"
path = "scripts/mean_reversion.lua"
auto_start = true
[scripts.parameters]
pair = "DOT/USD"
lookback = "15"
deviation = "1.5"
quantity = "5"

[[scripts]]
name = "ema_crossover_avax"
path = "examples/ema_crossover.lua"
auto_start = true
[scripts.parameters]
pair = "AVAX/USD"
fast_period = "12"
slow_period = "26"
quantity = "2"

[[scripts]]
name = "grid_trader_link"
path = "scripts/grid_trader.lua"
auto_start = true
[scripts.parameters]
pair = "LINK/USD"
grid_levels = "8"
grid_spacing = "0.03"
quantity = "5"

[[scripts]]
name = "mean_reversion_matic"
path = "scripts/mean_reversion.lua"
auto_start = true
[scripts.parameters]
pair = "MATIC/USD"
lookback = "25"
deviation = "2.5"
quantity = "200"

[[scripts]]
name = "momentum_doge"
path = "scripts/simple_momentum.lua"
auto_start = true
[scripts.parameters]
pair = "DOGE/USD"
threshold = "1.0"
quantity = "500"
